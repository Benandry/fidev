<form method="post" action="{{ path('app_transfertep_delete', {'id': transfertep.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transfertep.id) }}">
    <button class="btn">Delete</button>
</form>
