{{ include('header.html.twig') }}

{% block body %}
    <h1>Approbation</h1>

    <table class="table">
        <thead class="alert alert-success">
            <th>Code client</th>
            <th>Nom et Prenom</th>
            <th>Date d'inscription</th>
            <th>Date approbation</th>
        </thead>
        <tbody class="jumbotron jumbotron-fluid">
            <tr>
                <td>I{% for Agence in agences %}{{ "%02d" | format(Agence.id) }}{% endfor %}{{ "%06d" | format(clients.id) }}</td>
                <td>{{ clients.nomClient }} {{ clients.prenomClient }}</td>
                <td>{{ clients.dateInscription | date('d-m-y')}}</td>
                <td>{{ approuver_client.dateapprobation | date('d-m-y') }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_approuver_client_index') }}">back to list</a>

    <a href="{{ path('app_approuver_client_edit', {'id': approuver_client.id}) }}">edit</a>

    {{ include('Module_client/approuver_client/_delete_form.HTML.twig') }}
{% endblock %}

{{ include('footer.html.twig') }}
