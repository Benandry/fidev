{{ include('header.html.twig') }}

{% block body %}
    <a href="{{ path('app_liste_rougegroupe_index') }}"  class="btn btn-success"><i class="fa fa-search"></i> Recherche</a>

{{ form_start(form) }}
<div class="row " style="margin-left:250px;">
    <div class="col-sm-5 jumbotron jumbotron-fluid">
        {{ form_row(form.dateliste) }}
        {{ form_row(form.raison) }}
        {{ form_row(form.codegroupe) }}<br>
    <button class="btn btn-success">{{ button_label|default('Ajouter') }}</button>
    </div>
</div>

{{ form_end(form) }}
{% endblock %}

{{ include('footer.html.twig') }}

