{{ include('header.html.twig') }}

{% block body %}
    <h1></h1>

    {{ include('Module_client/suivi_client/_form.html.twig') }}

    <a href="{{ path('app_suivi_client_index') }}">back to list</a>
{% endblock %}

{{ include('footer.html.twig') }}
