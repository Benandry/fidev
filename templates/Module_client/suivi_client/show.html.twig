{{ include('header.html.twig') }}

{% block body %}
    <h1>Suivi Client</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ suivi_client.id }}</td>
            </tr>
            <tr>
                <th>DateEntre</th>
                <td>{{ suivi_client.DateEntre ? suivi_client.DateEntre|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateSortie</th>
                <td>{{ suivi_client.DateSortie ? suivi_client.DateSortie|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Utilisateur</th>
                <td>{{ suivi_client.Utilisateur }}</td>
            </tr>
            <tr>
                <th>Menu_utiliser</th>
                <td>{{ suivi_client.menuUtiliser }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_suivi_client_index') }}">back to list</a>

    <a href="{{ path('app_suivi_client_edit', {'id': suivi_client.id}) }}">edit</a>

    {{ include('Module_client/suivi_client/_delete_form.html.twig') }}
{% endblock %}

{{ include('footer.html.twig') }}
