<form method="post" action="{{ path('app_groupe_delete', {'id': Groupe.id}) }}" onsubmit="return confirm('Vous etes sur pour supprimer {{ Groupe.nomGroupe }} code {{ Groupe.codegroupe }} ??');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ Groupe.id) }}">
    <button class="btn btn-danger btn-sm"><i class="fa fa-trash"></i></button>
</form>
