{{ form_start(form) }}
    <div class="row me-5 ms-3 p-4">
        <div class="col-6 col-sm-6">
            {{ form_row(form.NomAgence) }}
            {{ form_row(form.AdressAgence) }}
        </div>
        <div class="col-6 col-sm-6">
            {{ form_row(form.commune) }}
        </div>
    </div>
    <div class="row ms-5">
        {{ form_row(form.Save) }}
    </div>
{{ form_end(form) }}
