{{ include('header.html.twig') }}

{% block body %}
    <h1>Agence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ agence.id }}</td>
            </tr>
            <tr>
                <th>Nom Agence</th>
                <td>{{ agence.NomAgence }}</td>
            </tr>
            <tr>
                <th>Adresse Agence</th>
                <td>{{ agence.AdressAgence }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_agence_index') }}">back to list</a>

    <a href="{{ path('app_agence_edit', {'id': agence.id}) }}">edit</a>

    {{ include('Module_client/agence/_delete_form.html.twig') }}
{% endblock %}

{{ include('footer.html.twig') }}
