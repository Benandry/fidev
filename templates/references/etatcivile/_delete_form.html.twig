<form method="post" action="{{ path('app_etatcivile_delete', {'id': etatcivile.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etatcivile.id) }}">
    <button class="btn">Delete</button>
</form>
