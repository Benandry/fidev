{{ include('header.html.twig') }}

{% block body %}
    <h1>Edit Langue</h1>

    {{ include('langue/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_langue_index') }}">back to list</a>

    {{ include('langue/_delete_form.html.twig') }}
{% endblock %}

{{ include('footer.html.twig') }}
