{{ include('header.html.twig') }}

{% block body %}
    <h1>Edit Etude</h1>

    {{ include('etude/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_etude_index') }}">back to list</a>

    {{ include('etude/_delete_form.html.twig') }}
{% endblock %}

{{ include('footer.html.twig') }}
