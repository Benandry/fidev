{# {{ include('header.html.twig') }}

{% block body %}
    <h1>Mobile</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mobile.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mobile_index') }}">back to list</a>

    <a href="{{ path('app_mobile_edit', {'id': mobile.id}) }}">edit</a>

    {{ include('mobile/_delete_form.html.twig') }}
{% endblock %}

{{ include('footer.html.twig') }} #}
