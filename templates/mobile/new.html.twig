{{ include('header.html.twig') }}

{% block body %}
    <h1>Create new Mobile</h1>

    {{ include('mobile/_form.html.twig') }}

    {# <a href="{{ path('app_mobile_index') }}">back to list</a> #}
{% endblock %}

{{ include('footer.html.twig') }}
