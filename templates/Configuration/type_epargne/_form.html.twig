{{ form_start(form) }}
    <div class="row " style="margin-left:200px;">
        <div class="col-sm-5 jumbotron jumbotron-fluid">
            {{ form_widget(form) }}<br>
                <button class="btn btn-success"> <i class="fa fa-plus"></i> {{ button_label|default('Ajouter') }}</button><a href="{{ path('app_type_epargne_index') }}" class="btn btn-success"><i class="fa fa-eye"></i> Voir Liste</a>

        </div>
    </div>
{{ form_end(form) }}
