{% extends "base.html.twig" %}

{% block title %} Type epargne : {{ type_epargne.NomTypeEp }}{% endblock %}
{% block head %}
   Type epargne : {{ type_epargne.NomTypeEp }}
{% endblock %}

{% block body %}
    <h1>Type Epargne</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_epargne.id }}</td>
            </tr>
            <tr>
                <th>NomTypeEp</th>
                <td>{{ type_epargne.NomTypeEp }}</td>
            </tr>
            <tr>
                <th>Abreviation</th>
                <td>{{ type_epargne.abreviation }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ type_epargne.description }}</td>
            </tr>
        </tbody>
    </table>


    <a href="{{ path('app_type_epargne_index') }}" class="btn btn-primary btn-sm"> <i class="fa fa-eye"></i> Voir  listes </a>

    <a href="{{ path('app_type_epargne_edit', {'id': type_epargne.id}) }}" class="btn btn-warning btn-sm" ><i class="fa fa-edit"></i> Modifier</a>

{% endblock %}