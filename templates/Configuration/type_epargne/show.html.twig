{{ include('header.html.twig') }}

{% block body %}
    <h1>Type Epargne</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_epargne.id }}</td>
            </tr>
            <tr>
                <th>NomTypeEp</th>
                <td>{{ type_epargne.NomTypeEp }}</td>
            </tr>
            <tr>
                <th>Abreviation</th>
                <td>{{ type_epargne.abreviation }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ type_epargne.description }}</td>
            </tr>
        </tbody>
    </table>


    <a href="{{ path('app_type_epargne_index') }}" class="btn btn-primary btn-sm">Voir  listes </a>

    <a href="{{ path('app_type_epargne_edit', {'id': type_epargne.id}) }}" class="btn btn-warning btn-sm" >Modifier</a>

{% endblock %}

{{ include('footer.html.twig') }}
