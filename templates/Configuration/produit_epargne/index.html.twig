{{ include('header.html.twig') }}

{% block body %}

    <table class="table table-bordered">
        <thead class="alert alert-success">
            <tr>
                <th>Type produit</th>
                <th>Code type</th>
                <th>Nom produit</th>
                <th>Code produit</th>
                <th>Description produit</th>
                <th>Etat</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody class="jumbotron jumbotron-fluid">
        {% for produit_epargne in produit_epargnes %}
            <tr>
                <td>{{ produit_epargne.typeep }}</td>
                <td>{{ produit_epargne.codetype }}</td>
                <td>{{ produit_epargne.nomproduit }}</td>
                <td>{{ "%03d" | format(produit_epargne.id) }}</td>
                <td>
                    <a class="btn btn-success">{{ produit_epargne.isdesactive ? 'Active':'Desactive' }}</a>
                </td>
                <td>
                    <a href="{{ path('app_produit_epargne_edit', {'id': produit_epargne.id}) }}" class="btn btn-success"><i class="fa fa-edit"></i> edit</a>
                </td>

                <td>
                    
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}

{{ include('footer.html.twig') }}
