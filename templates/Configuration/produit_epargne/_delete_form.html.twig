<form method="post" action="{{ path('app_produit_epargne_delete', {'id': produit_epargne.id}) }}" onsubmit="return confirm('Vous etes sur pour supprimer {{ produit_epargne.nomproduit  }} ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit_epargne.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
