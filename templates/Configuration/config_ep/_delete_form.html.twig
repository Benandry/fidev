<form method="post" action="{{ path('app_config_ep_delete', {'id': configs.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ configs.id) }}">
    <button class="btn">Delete</button>
</form>
