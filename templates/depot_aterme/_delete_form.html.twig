<form method="post" action="{{ path('app_depot_aterme_delete', {'id': depot_aterme.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ depot_aterme.id) }}">
    <button class="btn">Delete</button>
</form>
