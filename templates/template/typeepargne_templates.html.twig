{{ include('header.html.twig') }}

{% block body %}
   <a href="{{path('app_type_epargne_index')}}"><button class="btn btn-primary">Liste</button></a>
   <a href="{{path('app_type_epargne_new')}}"><button class="btn btn-primary">Nouveau Type</button></a>
{% endblock %}

{{ include('footer.html.twig') }}