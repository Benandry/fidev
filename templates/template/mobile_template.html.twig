{{ include('header.html.twig') }}

{% block body %}
   <a href="{{path('app_mobile_new')}}"><button class="btn btn-primary">Nouveau</button></a>
   <a href="{{path('app_mobile_index')}}"><button class="btn btn-primary">Liste</button></a>
{% endblock %}

{{ include('footer.html.twig') }}